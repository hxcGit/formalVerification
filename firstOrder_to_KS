1. 每条一阶逻辑保存成数据结构
struct Node
{
    String action;
    String Same;
    String pc;
    String pc';
}
    (1)如何识别出每一句：字符串处理，使用'或'符号分割
    (2)一条语句如何区分出相应字段，使用'与'符号进行分割


2. 全部的一阶逻辑放在一个一维结构体数组中
    (1)遍历访问全部一阶逻辑,找到某pc值与当前pc'值相等的结构体存入待处理数组handle[]
    (2)遍历handle数组
        - 若action是判断，则将对应pc'的数据结构存如待处理队列
        - 若action是赋值，则计算action中变量相应的改变，然后形成一个新的状态，同时输出对应的状态迁移关系


3. 输出的结果
